## Demo construct script

Import('env')

## Grab all the cpp files
sources = Glob('*.cpp')
object_list  = env.Object(source = sources)
env.Program(target = '#/bin/demo', source = object_list, LIBPATH = '#/lib', LIBS = ['cug', 'shp'] )
